{
  "name": "realpivot2-webinar",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "compile:live": "tsc --project tsconfig.live-server.json",
    "live": "tsc --project tsconfig.live-server.json --watch"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^7.3.6"
  }
}
